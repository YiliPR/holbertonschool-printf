#include "main.h"

int _printf(const char *format, ...)
{
        va_list args;
        int i = 0;
        int count = 0;

        va_start(args, fiormat);

/* NEXT STEP : Main Loop, handling format, handling %c, handling %s, handling %%, etc) */


/* In this file we will link the operations with the functions we want.
 * e.g.
 *
 * {"c", (*f)()};
 * {"s', (*f)()};
 * {"%", (*f)()};
 * {"d", (*f)()};
 * {"i", (*f)()};
 * {NULL,NULL}
 *
 * Keep in mind "d" and "i" are both intergers so link it with the integer function
 */
